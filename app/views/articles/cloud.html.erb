<h1>Weexus</h1>

<div id='tag_cloud'>
  <% tag_cloud Article.tag_counts, %w{s m l x} do |tag, css_class| %>
    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
  <% end %>
</div>

<p><%= link_to "View Experiences", articles_path %></p>
